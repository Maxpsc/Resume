# Resume

彭斯诚的个人简历

## 技术栈

- **React 18** - 使用最新的 React 18 和 Hooks
- **TypeScript** - 完整的类型支持
- **Vite** - 快速的开发服务器和构建工具
- **Less** - CSS 预处理器，支持 CSS Modules
- **pnpm** - 高效的包管理器

## 开发

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览生产构建
pnpm preview

# 代码检查
pnpm lint
```

## 项目结构

```
Resume/
├── src/
│   ├── components/       # React 组件
│   │   ├── Resume.tsx   # 主组件
│   │   ├── Content.tsx  # 内容组件
│   │   └── resume.module.less  # 样式文件
│   ├── types.ts         # TypeScript 类型定义
│   ├── main.tsx         # 应用入口
│   └── vite-env.d.ts    # Vite 类型声明
├── index.html           # HTML 模板
├── vite.config.ts       # Vite 配置
├── tsconfig.json        # TypeScript 配置
└── package.json         # 项目配置

```

## 重构说明

本项目已从旧的技术栈重构为现代化的开发环境：

### 变更内容

- ✅ **Webpack 1.x → Vite 5** - 更快的开发体验
- ✅ **Babel 6 → TypeScript 5** - 完整的类型安全
- ✅ **React 15 → React 18** - 使用最新的 React 特性
- ✅ **Class Components → Function Components + Hooks** - 现代化的组件写法
- ✅ **SCSS → Less + CSS Modules** - 更好的样式隔离
- ✅ **npm/yarn → pnpm** - 更高效的包管理

### 优势

- 🚀 **更快的开发速度** - Vite 的 HMR 几乎是即时的
- 📦 **更小的包体积** - 现代化的构建优化
- 🔒 **类型安全** - TypeScript 提供完整的类型检查
- 🎯 **更好的开发体验** - 现代化的工具链和最佳实践
- 🔧 **易于维护** - 清晰的项目结构和代码组织

## License

ISC
